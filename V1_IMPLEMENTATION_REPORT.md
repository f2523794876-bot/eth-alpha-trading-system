# ETH Alpha V1 实现报告

## 交付内容

主交付为 `eth-dynamic-trading-dashboard.html`：中文、单文件、无React/Vite/npm运行依赖、无需后端、无账户连接、无自动下单。

开发时使用 `v1-core.js` 保存可独立测试的纯函数源；`work/build-v1.js` 将其内嵌进正式HTML。Node仅用于开发和测试，不是用户运行依赖。

## 核心实现

- 六路公开REST：ETH/BTC × 15m/1h/4h，8秒超时、单次重试、部分失败标记。
- `closeTime` 推导 `isClosed`；实时价与 `confirmedPrice` 分离，Swing和正式突破只使用已收盘数据。
- 4小时战略状态、1小时结构状态、15分钟执行状态与BTC对应周期权限矩阵。
- ATR、EMA、Swing、异常K线剔除、Swing时效与聚类、两级支撑/压力区域。
- 毛/净盈亏比、交易成本、成交量质量、两级假突破、逐项透明评分。
- 独立试仓、专业加仓、禁止加仓原因、五类离场条件、动态止损/目标、蜻蜓拦截区。
- 三级数据健康、安全降级、严格手动输入校验和明确的手动观察模式；手动模式仅提供观察级近似支撑压力，不提供交易计划。
- localStorage决策日志，最多500条、先进先出。

## P0/P1修复

P0：`priorStructureHigh20/Low20` 从确认K线之前的已收盘历史计算。`isBreakout/isBreakdown` 只比较 `confirmedPrice` 与冻结结构；当前价格和当前K线不再污染触发基准。展示用 Swing/目标位与突破触发位分离。

P1：盈亏比无效值、止损失效、目标错误、已过目标和BTC冲突均有独立语义；恢复独立加仓字段；状态冲突由数据失效、确认失败、趋势确认、回踩、震荡、过渡的顺序解决；Swing加入时效、聚类、异常处理；数据缺失/陈旧/不同步时禁止实时建议。

## V1.1 增强（2026-07-13）

- 多头与空头待触发预案完整输出触发、进场、收盘确认、BTC、成交量、失效位、三目标、预计毛/净RR和未满足条件；预案始终标记“尚未触发，不构成当前交易建议”。
- 九项条件完成进度分为已满足、部分满足、未满足和硬性否决。
- 评分拆为多头机会、空头机会和市场可交易性；数据失效与权限否决始终高于评分。
- 位置距离输出美元、ATR、区间百分比和是否进入第一支撑/压力区。
- 本地15分钟倒计时保留实时价、确认价、盘中预警和正式收盘状态分离。
- 原生Canvas绘制最近50根15分钟K线、EMA5/10/20、实时价和支撑压力区；仅在有效交易方案存在时绘制交易线。
- 成本、资金和风险设置持久化；风险压力倍数只换算所需资金，不改变最大亏损、不提供杠杆建议。
- 增强决策日志包含数据源、市场类型、双币价格、三周期、区域、双向评分、证据、失效位/目标与状态变化，支持JSON/CSV导出和确认后清除。

## 范围合规

未实现 WebSocket、历史回测、主动提醒、模拟仓位和长期监控（V2/V3，其中模拟仓位已于v1.3阶段拆出单独实现，不再属于V3范围，见`V1_3_PAPER_TRADING_SPEC.md`）；未实现账户、密钥、自动下单、杠杆建议、强平计算、亏损摊平或盈利承诺。用户输入的杠杆倍数仅用于风险压力换算，不能扩大原始风险预算。旧文件未删除、移动或归档。

## 验收状态

自动测试与真实REST测试均通过；CEO已根据真实Chrome双击截图确认页面渲染、六路REST和浏览器CORS通过。2026-07-13验收修复完成后，新增回归测试覆盖区域来源标签、评分分档、无方案RR文案、拦截区位置和UI中文化。

V1.1初次完成时的历史测试记录已由后续复审回归覆盖；当前统一口径见本报告末尾。

## Claude独立复审修复（2026-07-13）

- 正式支撑压力候选、区域角色、止损、目标、正式RR和双向预案全部改为基于 `confirmedPrice` 与已收盘结构；实时价仅用于展示、距离、图表实时线和盘中预警。
- 新增统一 `invalidateDashboard`：整体失败、关键周期缺失、陈旧或时间不同步时，立即清空/失效全部交易面板和Canvas交易标记，只保留带时间戳的最后已知价格；恢复后重新构建。
- 新增 `assessHardBlocks`，输出 `blocked` 与 `blockReasons`。方向评分在硬性否决时显示“不可执行”，原始分仅保留为审计字段。
- 历史压力转支撑使用集中配置 `SR_ROLE_TOLERANCE_ATR=0.3`，并要求已收盘Swing High、后续有效收盘突破、区域回踩、收盘守住和当前结构仍在上方的完整链条。
- CSV导出对 `=`、`+`、`-`、`@` 开头内容增加公式注入防护。
- 市场可交易性按数据健康、结构清晰、区域空间、净RR、BTC、成交量和假突破风险独立加权，不再取多空分平均；硬性否决时归零。
- 加仓文案明确系统未连接持仓、无法验证浮盈，必须由用户结合真实入场价确认。

## Claude第三轮定向修复（2026-07-13）

- 手动观察模式从实时数据失效守卫中明确豁免，仅展示用户输入及观察级近似支撑压力；不计算或展示止损、目标、盈亏比、仓位、加仓条件或可执行评分，且不写入真实行情日志。
- 统一失效覆盖总评分、确认价、盘中预警、收盘状态、警告和评分说明；保留价格时同时显示最后更新时间、非实时及不可用于交易决策。恢复后清除失效样式并重建全部字段。
- 总评分区分 `rawTotal` 与 `effectiveTotal`；数据失效、陈旧、假突破失败或多周期硬冲突时，可执行总评分为不可执行，原始分仅供内部审计。
- CSV仅对原始字符串的公式风险前缀进行防护，数值负数和负小数保持数值语义。
- 支撑压力空间从实际区域、ATR、确认价、计划进场与失效位计算，并将可用ATR、最低门槛和判断原因传入生产评分路径。
- CSV中的 `null` 与 `undefined` 导出为空单元格；手动模式最终统一定义为“仅提供观察级近似支撑压力，不提供交易计划”。

自动化测试：101项通过，0失败。

人工浏览器验收：1项，已由CEO完成真实Chrome双击验收；本轮按要求不重复。

总验收记录：101项自动化测试 + 1项人工浏览器验收；不得表述为102项自动测试。未加入WebSocket、回测、账户或自动下单能力。
