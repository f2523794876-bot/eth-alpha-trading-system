# PROJECT_AUDIT.md — ETH Alpha 动态短线交易决策系统 · 项目审计报告

审计人：Claude Code（策略架构师 / 技术审计负责人）
审计目录：`/home/ubuntu/eth-trading-dashboard/`
审计方式：全文件通读 + Node.js 沙盒直连 Binance 真实 API 验证 + 合成K线单元测试复现关键逻辑分支
版本：v2.1（第三轮修订：CEO第二轮验收后，V1范围从"15分钟单周期"改为"三周期REST决策核心"，见第0节与第11节；未重新审计代码，本轮仅为范围文字修订）
审计日期：见本文件所在 git/文件系统时间戳（本目录未启用 git 版本管理，见「代码风险」第6条）

---

## 0. 第二轮范围变更说明（v2.0）

CEO（ChatGPT）已完成第一轮验收，确认以下结论有效并**必须保留、不得删除**：
1. 动态压力/支撑突破判定存在 P0 级自指回避缺陷（第4.1节）；
2. 突破基准必须排除当前K线并使用冻结结构（第4.1节修复方向）；
3. 盈亏比边界处理不完整（第4.2节）；
4. 动态版缺少独立加仓条件（第4.3节）；
5. 状态机缺少保持/退出/优先级/冲突解决（第4.4节）；
6. Swing点需要时效性、聚类和异常处理（第4.5节）；
7. 数据需要陈旧检查、安全降级和自动化测试（第3节、第6节）。

董事长与CEO在第一轮验收后做出**范围扩大决定**，本文档与另外三份文档（STRATEGY_SPEC.md / CODEX_IMPLEMENTATION_TASK.md / ACCEPTANCE_TESTS.md）已同步更新，新增以下十二项能力要求，详见 STRATEGY_SPEC.md 对应章节：

1. 三周期架构（4小时战略方向 + 1小时趋势结构 + 15分钟执行位置 + BTC对应周期联动）；
2. 严格分离已收盘/未收盘K线（正式状态判定只用已收盘数据，未收盘K线只产生盘中预警）；
3. 实时数据架构（REST初始化+断线补偿 / WebSocket实时更新 / 自动重连退避 / 三级数据健康状态）；
4. 支撑压力从单点改为**区域**（Swing聚类 + ATR决定宽度 + 区域上下边界 + 可信度）；
5. 交易成本后的净盈亏比（手续费/价差/滑点，预留资金费率字段）；
6. 成交量质量（主动买入比例、放量滞涨、缩量回踩、突破后量能持续性）；
7. 透明信号评分（0-100分，逐项加减分可追溯，不替代硬性安全规则）；
8. 历史行情实验室（多年数据回放、样本外验证、参数敏感性测试——**撤销第一轮"历史回测不在范围内"的结论**）；
9. 专业试仓/加仓/离场规则强化（风险预算上限、移动止损）；
10. 决策日志与条件提醒；
11. 对应的新增测试类别（ACCEPTANCE_TESTS.md 第二轮新增13类）；
12. 项目边界补充（禁止杠杆、禁止黑箱AI自主决策、不扩展到其他币种）。

**重要声明：本次更新前的四份文档（v1.0）均基于"仅15分钟单周期"的假设，其中 STRATEGY_SPEC.md 曾声明"适用范围：ETH/USDT、BTC/USDT，15分钟K线"，CODEX_IMPLEMENTATION_TASK.md 曾声明"禁止把...历史回测框架...实现进本阶段代码"——这两处表述在 v2.0 中已被推翻并删除，不再适用。** 除此之外，v1.0 中记录的全部审计发现（尤其是上面复述的7条）在 v2.0 中原样保留，不因范围扩大而失效——P0级缺陷仍然是P0级缺陷，且现在必须在15分钟执行层和更高周期上分别验证。

**当前状态：四份文档尚未纳入第二轮范围之前，不能交付Codex；本次更新完成后是否可以交付，见本文件末尾新增的「第二轮结论」。**

---

## 1. 当前文件清单

| 文件 | 行数 | 角色 | 状态 |
|---|---|---|---|
| `index.html` | 126 | 三文件版UI骨架，引用 `style.css` + `app.js` | **过时，本轮暂不删除/移动/归档（CEO第二轮验收决定）** |
| `style.css` | 322 | 三文件版样式表 | **过时，本轮暂不删除/移动/归档** |
| `app.js` | 391 | 三文件版决策逻辑（固定关键位） | **过时，本轮暂不删除/移动/归档** |
| `eth-trading-dashboard.html` | 816 | 单文件版，与上述三文件**功能完全重复**（同一套固定关键位逻辑内联为单文件） | **过时，本轮暂不删除/移动/归档** |
| `eth-dynamic-trading-dashboard.html` | 912 | 单文件版，接入 Binance 15分钟K线，实现 ATR/EMA/Swing/动态支撑压力/盈亏比/手动兜底 | **当前最先进版本，但存在P0级逻辑缺陷（见第4节），不可直接进入下一阶段开发** |

未发现 `PROJECT_AUDIT.md` / `STRATEGY_SPEC.md` / `CODEX_IMPLEMENTATION_TASK.md` / `ACCEPTANCE_TESTS.md` 等既有文档，本次为首次建档。
目录未初始化 git（`git status` 报 `not a git repository`），历史版本仅能靠文件名/文件系统 mtime 区分，无法追溯变更历史。已在第4节列为代码风险。

---

## 2. 已有功能盘点

### 2.1 `index.html` + `style.css` + `app.js`（三文件固定关键位版）
- 通过 `GET /api/v3/ticker/price?symbol=ETHUSDT|BTCUSDT` 每 5 秒轮询最新单一价格（**不是K线，没有OHLC，没有历史K线序列**）。
- 维护一个 15 分钟滚动价格窗口（内存数组，刷新页面即清空），用简单首尾百分比涨跌判断 BTC 趋势（涨/跌/横盘阈值 ±0.15%）。
- 10 个**写死的关键位**（前低支撑1747、弱势支撑1750、结构转换位1766、多空分界1770、换手区1780-1786、心理位1800、上方压力1834、目标1850/1870），可在页面手动改。
- if/else 顺序决策树，输出六态之一：`BULL_CONFIRMATION / BULL_PULLBACK / BEAR_CONTINUATION / TRANSITION_WATCH / RANGE_CHOP / STAND_ASIDE`（与本阶段 STRATEGY_SPEC 要求的六态**命名完全一致**，可视为策略命名的既有共识，逻辑需重写）。
- 每态输出：建议文案、仓位建议、风险等级、试仓区/加仓条件/止损/目标（仅多空态）、观望原因/等待条件（仅观望态）。
- API失败时的手动价格输入兜底（仅ETH/BTC两个数字，无高低点/ATR等）。

### 2.2 `eth-trading-dashboard.html`（单文件固定关键位版）
逐字对照后确认：**这是 2.1 三个文件的内联合并版，决策逻辑、常量、阈值、状态机结构逐行相同**，只是把 CSS 和 JS 塞进一个 `<html>` 文件。没有任何功能差异。

### 2.3 `eth-dynamic-trading-dashboard.html`（当前最新单文件动态版，本人上一轮会话产出）
- 接入 `GET /api/v3/klines?symbol=ETHUSDT&interval=15m&limit=100`（ETH、BTC 各一路），20秒轮询。
- 已实现：EMA(5/10/20)、ATR(14，简单TR均值非Wilder平滑)、最近20/50根K线高低点、Swing High/Low 识别（前后各2根确认）、动态第一/第二压力支撑、突破/跌破 ATR 目标位（×0.5/1/1.5）、动态止损（支撑/压力 ± ATR×0.3）、盈亏比计算与三档提示文案、市场状态判断（多头/空头/震荡/假突破风险/未知过渡）、蜻蜓拦截区文案、人工参考位（明确标注"不参与动态计算"）、API失败→手动输入兜底（ETH价/BTC价/最近高点/最近低点/20根高点/20根低点，共6项，与本次需求条目一致）。
- 已用 Node.js 直连 Binance 验证：100根K线正常返回，ETH价1806.71、ATR(14)=8.04，计算链路无崩溃、无 NaN。**但该验证只覆盖了价格处于支撑压力"中间区域"这一条代码路径，未覆盖突破/跌破分支**（见第4节，这直接导致P0缺陷在上次验证中未被发现）。

---

## 3. 缺失功能（对照本次十七条需求逐项核对）

| 需求条目 | 现状 |
|---|---|
| §3 数据获取失败/数据过期/K线数量不足时"安全降级，不能继续输出伪装成实时数据的建议" | **缺失**。`eth-dynamic-trading-dashboard.html` 只处理"API请求失败"这一种异常，未检查K线根数是否满足计算所需最小长度（EMA20/ATR14/Swing至少需要约25根），未检查K线时间戳是否陈旧（`closeTime` 距当前时间过久说明数据可能是缓存/僵死连接）。 |
| §6 多单/空单失效位 = 支撑/压力 ± ATR×0.3，是否合理需审查 | 代码里已按此公式实现，但**未做"该系数是否合理"的技术论证**，需在 STRATEGY_SPEC 中给出分析结论。 |
| §7 Swing异常情况（无上方Swing/无下方Swing/距离过近/已过期/多点聚集/异常K线跳空） | **6种异常中只处理了"无Swing"这1种**（回退到 high20/low20），其余5种（过近、过期、聚集、跳空）完全未处理。 |
| §8 六状态机的"进入/保持/退出条件、优先级、冲突解决顺序、伪代码、可自动执行测试用例" | **只有"进入条件"的简化实现，没有保持条件、没有退出条件、没有显式优先级表、没有冲突解决顺序说明、没有伪代码文档、没有自动化测试**。当前实现是隐式的 if/else 顺序耦合优先级，未文档化，不可维护，不可测试。 |
| §9 假突破"风险提示"与"确认失败"两级 | **未分级**，当前只有一个布尔 `falseBreakoutRisk`，且未使用成交量放大/收盘回落区间/突破持续K线数/长上下影线等六项判据中的后三项。 |
| §10 加仓条件作为独立字段输出 | **缺失**。旧版 `app.js` 曾有 `addCondition` 字段，新版 `eth-dynamic-trading-dashboard.html` 的 `renderPlan()` 把这个字段**弄丢了**，是功能回退。 |
| §11 盈亏比8种边界情况（风险=0、目标反向、价格已超目标、支撑压力缺失、负盈亏比、数据无效、多空双差、盈亏比好但BTC冲突） | **只处理了"风险≤0返回null"一种**，其余7种边界情况均未特殊处理，会产生误导性输出（见第4节3）。 |
| §12 蜻蜓模型转成"可由程序计算的规则" | 已完成为条件分支（非纯比喻文案），基本满足，但判断依据仍主要复用市场状态与买卖方向标记，缺少"当前最佳拦截区"独立于市场状态的**显式坐标计算函数**，需要在 STRATEGY_SPEC 中形式化。 |
| §14 单文件技术边界16项 | 结构性全部满足（无React/Vite/npm、中文界面、内置K线API、支持手动回退、支持ATR/EMA/Swing/动态支撑压力/盈亏比/BTC联动、人工关键位仅参考）。**唯独"API失败时页面不能崩溃"和"数据过期不能伪装成实时数据"这两条因为第4节的P0缺陷和第3节的数据陈旧检测缺失，不能视为已达标**。 |

### 3.1 第二轮新增范围与现有代码的差距（v2.0新增）

以下12项是本次范围扩大新增的要求，现有 `eth-dynamic-trading-dashboard.html` **全部未实现**，与第一轮的"局部函数级缺陷"性质不同——这些是**架构级缺口**，不是修修补补能解决的：

| 新增要求 | 现有代码状态 |
|---|---|
| 三周期架构（4h/1h/15m + 权限矩阵） | 现有代码只拉取15分钟K线一路，没有4小时、1小时数据源，没有跨周期状态机，没有信号权限矩阵。 |
| 已收盘/未收盘K线分离 | 现有代码把 `klines[N-1]` 无差别当作"当前价"来源参与所有判断（含Swing识别、突破判定），STRATEGY_SPEC v1.0 §0 甚至明确写"不区分是否已收盘"——**这条v1.0表述本身在v2.0中已被推翻**，属于本次必须修正的设计缺陷。 |
| 实时数据架构（REST+WS） | 现有代码只有 REST 轮询（20秒定时器），没有 WebSocket，没有重连退避，没有连接状态/数据健康分级。 |
| 支撑压力区域化 | 现有 `firstResistance`/`firstSupport` 是单点，没有聚类宽度、没有区域边界、没有可信度分级展示（v1.0 STRATEGY_SPEC §5.2第5项已有"聚集去重"的雏形，但输出的仍是代表点，不是区域）。 |
| 净盈亏比（交易成本） | 现有 `calcRR()` 只算毛盈亏比，完全没有手续费/价差/滑点概念。 |
| 成交量质量 | 现有只有单一 `volumeRatio`（当前量/20均量），没有主动买入比例、放量滞涨、缩量回踩、突破后量能持续性等细分判据。 |
| 透明信号评分 | 现有完全没有评分系统，只有状态文案。 |
| 历史行情实验室 | 现有完全没有回测代码或数据管道，v1.0 CODEX_IMPLEMENTATION_TASK.md 曾明确列为"不做"，v2.0 已撤销此结论。 |
| 决策日志与条件提醒 | 现有没有任何持久化记录，每次刷新即丢弃上一次的判断依据。 |

这意味着**第二轮不是在第一轮的P0/P1修复基础上"顺带"加几个字段，而是需要在修复P0之后，叠加一层数量级更大的架构工作**。CODEX_IMPLEMENTATION_TASK.md 已按此现实拆分为 V1/V2/V3 三个阶段（见该文档第1节），避免第一个交付周期就试图吞下全部范围。

---

## 4. 代码风险 / 逻辑错误（重点，按严重程度排列）

### 4.1【P0】动态压力/支撑的突破判定存在"自指回避"缺陷，导致突破/跌破信号几乎永远无法触发

**文件**：`eth-dynamic-trading-dashboard.html`，`analyzeKlines()` 与 `buildAdvice()`

**问题本质**：`high20` / `low20` 取自 `klines.slice(-20)`，**包含当前（最新）这一根K线自己**；`firstResistance` / `firstSupport` 取自 `swings.highs/lows.filter(s => s.price > / < price)`，即"价格上方/下方最近的摆动点"，**每一刻都用当前价格重新过滤**。

这两个设计叠加，造成两个连锁问题：

1. **价格突破的瞬间，用来判定"是否突破"的参照位会跟着价格一起"跑"。** 一旦价格越过原先最近的 Swing High，该 Swing High 就不再满足"高于当前价格"，于是被过滤掉，`firstResistance` 立刻跳到更远一档的 Swing High——而 `price > firstResistance` 这个判断条件几乎不可能为真（因为 `firstResistance` 的定义本身保证它总是在价格上方）。
2. `high20` 把当前这根K线自己的最高价也计入了参照窗口，一根真正创出新高的突破K线，其自身的 high 天然会成为 `high20` 的新最大值，导致 `price(close) > high20` 同样几乎不可能成立——用一根K线的收盘价去比较"包含它自己在内"的20根最高点，结构上就很难触发。

**复现证据**（合成K线单元测试，见 `/tmp/.../scratchpad/synthtest.js`，构造了一段40根震荡（1770-1800区间，其中1根摆动高点1810）后接一根放量突破K线（收盘1815，最高1818）和一根延续K线（收盘1816）：

```
price 1816  firstResistance 1820  high20 1820  breakoutHigh20 false
bias: neutral
advice: 当前价格处于动态支撑 1,766.01 与动态压力 1,820.00 中间。
```

价格已经明确突破 1810 的摆动高点，且是放量突破，系统却判定为"处于支撑压力中间，赔率一般"——**这正是本任务要根治的"旧系统固定点位"问题的动态版变体：不是点位固定不动，而是点位会诡异地跟着价格同步移动，同样导致突破信号失灭。**

**为什么上一轮 Node 验证没发现**：上次验证时 ETH 实盘价格恰好落在支撑压力"中间区域"，没有走到突破分支，所以没暴露问题。这说明**用实盘行情做验证不可靠，必须用构造好的合成K线覆盖每一个状态分支**（已写入 ACCEPTANCE_TESTS.md 的强制要求）。

**必须的修复方向**（写入 STRATEGY_SPEC.md 第2节，作为 Codex 开发的强制约束）：
- `high20`/`low20`/`high50`/`low50` 必须用**排除当前最新一根K线**的窗口计算（即 `klines.slice(-21, -1)` 这样的"已收盘的历史结构"），当前价格只用来与这个"冻结"的结构做比较，不能反过来污染结构本身。
- 突破/跌破判定**不能用"当前价格上方最近Swing"这种自过滤定义**做触发条件，必须改为：将"最近一次被有效突破的压力位/支撑位"作为独立状态量跟踪（例如记录"上一根K线收盘时的 firstResistance 快照"，本根K线价格若超过该快照即视为突破），或直接用排除当前K线的 `high20` 作为唯一触发基准，`firstResistance`（含当前K线的最新一档）只作为"突破后下一目标"的展示字段，不作为触发条件。

### 4.2【P1】盈亏比计算 7 类边界情况未处理，可能产生误导性数值

**文件**：`eth-dynamic-trading-dashboard.html`，`calcRR()`

当前实现：
```js
const risk = price - stop;
const reward = targets[0] - price;
return risk > 0 ? reward / risk : null;
```
- `reward` 为负（目标位已经在价格后方，即"价格已经超过目标"）时，函数仍会返回一个负数交给 `rrMessage()`，而 `rrMessage()` 没有对负数做特殊判断，会落入 `rr < 1.5` 分支，显示"赔率一般，不建议重仓"——**这是错误的诊断结论**，正确结论应该是"当前价格已超过第一目标，需要重新评估目标位/落袋"。
- `risk <= 0`（止损已经被击穿）只返回 `null`，UI 显示"数据不足，暂无法计算盈亏比"，**语义误导**——真实原因是止损失效，不是数据不足。
- 未处理：支撑或压力缺失（理论上 `firstResistance`/`firstSupport` 会 fallback 到 high20/low20，恒有值，但 fallback 之后的值是否仍具有"失效位"的物理意义未经论证）、多空双向盈亏比都很差时应给出"当前不适合任何方向"的合并结论（目前只会单独展示一个方向的RR，不会显式提示"另一方向也差"）、盈亏比达标但 BTC 方向冲突时应降级提示（目前 `rrMessage()` 完全不感知 BTC 状态，只看数字）。

### 4.3【P1】"加仓条件"字段在动态版中丢失，是相对旧版的功能回退

`app.js` 的 `evaluateDecision()` 对多/空态都输出了 `addCondition`（如"价格回踩不破且BTC同步走强"）。`eth-dynamic-trading-dashboard.html` 的 `renderPlan()` 只输出了"方向参考/建议仓位/试仓区/止损条件/止盈目标"，**没有独立的加仓条件字段**，直接违反本次需求 §10 第10条"加仓条件"必须作为独立输出项。

### 4.4【P1】状态机没有文档化的优先级与冲突解决顺序

`classifyState()` 用顺序 if/else（假突破 → 多头 → 空头 → 震荡 → 未知）隐式定义优先级，**没有任何注释或文档说明为什么是这个顺序**，也没有处理"多头条件和假突破条件同时成立但方向相反"这种冲突（例如强势突破放量但同时 EMA 还未完全排列好）应该如何取舍。本次需求 §8 明确要求显式优先级表 + 冲突解决顺序说明，当前完全缺失，必须在 STRATEGY_SPEC 中补全。

### 4.5【P2】Swing 点没有"新鲜度"和"聚集去重"处理

`findSwings()` 对100根K线全量扫描，一个95根K线之前形成的摆动高点和1根K线之前形成的摆动高点被**同等看待**，只要是"价格上方最近的"就会被选为 `firstResistance`，不考虑该点位是否已经"过期"（例如90根K线=22.5小时之前的高点，在当前波动率下参考意义存疑）。同样没有对"两个Swing点位价格非常接近（例如相差<0.1×ATR）"做合并去重，可能导致 `firstResistance` 和 `secondResistance` 实际上是同一个价格区域的两个几乎重复的点。

### 4.6【P2】ATR 计算用简单算术平均而非 Wilder 平滑

`calcATR()` 对最近14个 True Range 值做简单算术平均。行业标准 ATR（Wilder, 1978原始定义）使用递归指数平滑（类似EMA但系数为1/N）。两者数值会有差异，尤其在波动率快速变化时更明显。当前实现不是"错误"，但需要在 STRATEGY_SPEC 中明确写清楚"这是简化版ATR，不是Wilder-ATR"，避免后续开发者或使用者误以为两者等价。

### 4.7【P2】未启用版本管理

目录内没有 `.git`，无法追溯"哪个改动引入了P0缺陷""哪次改动是回归"。建议 Codex 开发阶段第一步先 `git init` 并提交当前基线。**（第三轮更新：CEO第二轮验收已明确授权Codex执行此操作，不再需要事先询问用户，不再是阻塞项。）**

---

## 5. 代码重复

`index.html`+`style.css`+`app.js`（三文件）与 `eth-trading-dashboard.html`（单文件）是**同一套固定关键位系统的两份物理拷贝**，逻辑、常量、阈值逐行一致。两者都是本任务要淘汰的"旧系统"。**（第三轮更新：CEO第二轮验收决定本轮暂时不删除、不移动、不归档这两份旧文件，原"移入legacy/子目录"的建议已撤销，维持原样即可。）**

---

## 6. 为什么部分实时数据不能显示（诚实说明，不夸大验证结论）

1. **本次及上一轮的"验证成功"都只在 Node.js 服务端环境完成**。Node 的 `fetch` 不受浏览器 CORS 同源策略约束，**不能证明双击本地HTML文件后，浏览器里的 `fetch('https://api.binance.com/...')` 一定能成功**。这是一个尚未在真实浏览器环境验证的开放问题。
2. Binance 现货公开行情接口 `api.binance.com` 历史上对**部分地区（包括美国IP）返回 HTTP 451（因法律原因不可用）**，与CORS无关，是地域合规限制。如果用户或其网络出口位于受限地区，无论前端代码怎么写都无法直连成功，**手动输入兜底必须被当作"常态可能路径"而不是"极端兜底"来设计**。
3. 部分企业网络代理、广告拦截类浏览器插件会屏蔽 `*.binance.com` 域名。
4. `file://` 协议页面在部分浏览器（尤其是 Safari 默认安全设置）下对跨域 `fetch` 的限制比 `https://` 页面更严格，具体行为随浏览器版本变化，需要 Codex 阶段在真实 Chrome/Edge/Safari 上分别用双击文件方式实测，不能只靠本地 `http-server` 起服务测试后就认为双击场景等效。
5. 旧的 `app.js` 每 5 秒轮询 `ticker/price`，`eth-dynamic-trading-dashboard.html` 每 20 秒轮询 `klines`（weight更高但频率更低），两者都**没有失败重试退避（backoff）和限流保护**，长时间挂在后台标签页可能触发 Binance 的 429/418 限流封禁，进一步造成"数据不显示"。
6. 浏览器对**后台（非激活）标签页的 `setInterval` 会做节流**，长时间挂在后台的标签页刷新频率会显著变慢甚至暂停，回到前台时应主动补一次请求（`visibilitychange` 事件），当前两个版本都未处理。

## 7. 单文件双击运行可能受到的浏览器跨域/网络限制（汇总，供 STRATEGY_SPEC / CODEX 引用）

- CORS（跨域资源共享）：Binance 公开行情接口通常带 `Access-Control-Allow-Origin: *`，但**必须在真实浏览器里验证**，不能假定。
- 地域合规限制（HTTP 451）：与技术实现无关，无法通过代码规避，只能靠手动输入兜底。
- 网络环境限制：公司防火墙、广告拦截插件、DNS污染。
- `file://` 源的安全策略差异：不同浏览器、不同版本行为不一致。
- 限流/封禁：高频轮询 + 无退避机制。
- 后台标签页节流：影响"实时性"，不是"失败"，但会造成数据延迟的假象。

---

## 8. 必须由 Codex 开发阶段解决的问题（清单，详见 CODEX_IMPLEMENTATION_TASK.md）

1. 修复 §4.1 的 P0 突破/跌破自指缺陷（最高优先级，阻塞其他一切交易建议逻辑）。
2. 补全 §4.2 盈亏比 7 类边界情况的专门文案与分支。
3. 补回 §4.3 加仓条件独立字段。
4. 补全 §4.4 状态机的显式优先级表、保持条件、退出条件、冲突解决顺序（STRATEGY_SPEC 已给出规范，Codex 按规范实现即可）。
5. 补全 §4.5 Swing 六种异常情况的显式处理（详见 STRATEGY_SPEC 第5节）。
6. 补全 §3 的 K 线数量不足 / 数据陈旧安全降级检测。
7. 在真实浏览器（非Node、非http-server代理）**双击本地文件**的方式下实测 Binance API 的 CORS 与地域可用性（覆盖全部6路REST请求，不只15分钟一路），并把结果写回文档，不能假设。**（第三轮更新：CEO已明确此项由Codex在V1阶段直接完成，不是需要另行批准的前置阻塞条件。）**
8. 补加请求失败退避重试与限流保护，补加 `visibilitychange` 唤醒重新拉取（V1为REST轮询版，见STRATEGY_SPEC §14）。
9. ~~决定旧文件（三文件版 + 旧单文件版）的去留~~ **（第三轮更新：CEO已决定本轮暂不删除/移动/归档，此项已解决，不再是待办。）**
10. 补齐自动化测试（ACCEPTANCE_TESTS.md 已给出用例设计，需要 Codex 在实现阶段接入可执行的测试脚本，例如用 Node + 合成K线数据跑断言，而不是只能靠人工点开页面观察）。
11.（v2.0新增，v2.1确认为V1必做）实现三周期数据拉取与4h/1h/15m状态机权限矩阵（STRATEGY_SPEC.md §12）。
12.（v2.0新增）实现已收盘/未收盘K线分离，修正v1.0中"不区分是否收盘"的表述与实现（STRATEGY_SPEC.md §13）。
13.（v2.1修正：原计划REST+WebSocket混合实现已改为分阶段）V1只实现REST定时轮询版的三级数据健康状态（STRATEGY_SPEC.md §14 REST部分），WebSocket部分推迟到V3。
14.（v2.0新增）实现支撑压力区域化展示（STRATEGY_SPEC.md §2扩展）。
15.（v2.0新增，此处修正原文误引用的章节号）实现净盈亏比（STRATEGY_SPEC.md §10.1a）、成交量质量细分（§16）、透明评分（§17）。
16.（v2.0新增）按 V1/V2/V3 分阶段推进，V2阶段设计并搭建历史回测数据管道（STRATEGY_SPEC.md §17，CODEX_IMPLEMENTATION_TASK.md 第1节）。
17.（v2.0新增）实现决策日志与条件提醒（STRATEGY_SPEC.md §16）。

---

## 9. 推荐开发基础

**推荐以 `eth-dynamic-trading-dashboard.html` 作为 V1 阶段开发基础**，理由：
- 已具备15分钟K线接入、ATR/EMA/Swing、手动兜底、盈亏比框架等本任务要求的核心结构性组件，重写代价远低于从零开始。
- 已发现的 P0/P1 缺陷都是**局部函数级别的问题**（`analyzeKlines` 的窗口定义、`buildAdvice` 的触发条件、`calcRR` 的边界分支、`renderPlan` 的字段），不需要推翻整体架构，STRATEGY_SPEC.md 已给出具体的修复算法定义。

**不推荐**以三文件版或 `eth-trading-dashboard.html`（固定关键位版）为基础，两者架构上就不支持K线序列，属于需要整体推翻的旧范式，仅可作为"历史参考/UI外观对照"保留。

**在正式动手改代码前，必须先完成 STRATEGY_SPEC.md 中定义的算法修正，尤其是 §4.1 的突破判定重构**，否则会在旧的错误定义上继续堆叠新功能，返工代价更高。

**（v2.0原说明，已被v2.1推翻，保留于此仅作历史记录）**：第3.1节曾指出三周期架构/WS实时/区域化/回测实验室四项都是架构级新增，v2.0据此把V1范围限定为"15分钟单周期"。**CEO第二轮验收明确不批准这个限定**：三周期架构（REST版）现在是V1的硬性交付项，只有WebSocket实时化和历史回测实验室继续留在V2/V3。

**（v2.1现行结论）** V1 阶段的范围是：以 `eth-dynamic-trading-dashboard.html` 为基础，完成 P0/P1 修复 + ETH/BTC 三周期（15分钟/1小时/4小时）REST K线接入与三周期决策（4小时战略方向、1小时趋势结构、15分钟执行位置、BTC对应周期联动、信号权限矩阵）+ 已收盘/未收盘K线分离 + 区域化支撑压力 + 毛/净盈亏比 + 成交量质量 + 透明信号评分（真实三周期口径）+ 试仓/加仓/离场条件 + 蜻蜓最佳拦截区 + 数据健康与安全降级（REST版）+ 决策日志 + 合成K线自动测试。V1**不要求**WebSocket、条件提醒推送、模拟仓位、长期运行监控、历史回测引擎，这五项留给V2（历史回测）/V3（其余四项）。完整的17项清单与工程实现顺序见 CODEX_IMPLEMENTATION_TASK.md 第1、5节。

---

## 10. 第二轮结论（v2.0，已被第三轮部分修正，见下方"11. 第三轮结论"）

- 第一轮记录的7条核心审计结论（P0突破缺陷、冻结结构修复方向、盈亏比7类边界、加仓条件缺失、状态机优先级缺失、Swing异常处理缺失、数据安全降级缺失）**全部保留，未被本次范围扩大稀释或覆盖**。
- 第二轮新增的12项范围要求已在 STRATEGY_SPEC.md / CODEX_IMPLEMENTATION_TASK.md / ACCEPTANCE_TESTS.md 中补齐规范与测试设计。
- ~~本文档连同另外三份文档，在完成本次更新后，可以交付 Codex 开始 V1 阶段开发（15分钟单周期...）~~ **此条V1范围表述已在第三轮被CEO推翻，见第11节。**

## 11. 第三轮结论（v2.1，当前有效）

- CEO第二轮验收**不批准**V1停留在15分钟单周期，V1范围正式扩大为"三周期REST决策核心"（17项，见第9节v2.1现行结论）。
- 第一轮7条核心结论、第二轮12项新增范围要求，**均未因本次范围调整而被推翻或削弱**，本次只调整"V1现在要不要做"的边界，不调整"该做什么、算法该怎么定义"的内容——STRATEGY_SPEC.md第12-20节的算法定义本身没有变化。
- **本文档连同另外三份文档，在完成第三轮更新后，可以交付 Codex 开始 V1 阶段开发**，V1范围是三周期REST决策核心（第9节17项），WebSocket/历史回测引擎/条件提醒推送/模拟仓位/长期运行监控留给V2/V3，Codex在V1阶段不应提前实现这五项。
- **阻塞项已减少为一条**：Binance API 在真实浏览器下的 CORS/地域可用性尚未实测——但这条**不再是需要用户先批准才能开工的前置阻塞**，CEO已明确授权由Codex在V1开发过程中直接实测并记录结果（第8节第7条）。旧文件去留、git初始化两项均已由CEO第二轮验收决定，不再是阻塞项。
