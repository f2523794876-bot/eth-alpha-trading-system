# ETH Alpha V1.3.1 自动交易门禁诊断实现报告

## 根因

生产页面每次成功刷新都会派发 `v11decision`，V1.2预测也确实传入V1.3，因此问题不在REST刷新或自定义事件没有执行。真正的静默断点是 `recordSignalIfEligible()`：它要求 `worthBetting===true`、无硬性否决且方向明确，否则立即返回且不建立任何档案。后续影子验证和自动引擎都以建议档案为输入，所以严格许可未通过时三个区域会同时长期为零。自动引擎对“没有候选建议”的路径又没有诊断记录，页面只能显示“暂无”。

## 修复

- 保持V1.1交易许可、BTC、多周期、假突破、数据健康、净盈亏比、500 USDT账户和风险预算不变。
- 建议档案拆分为：
  - `OBSERVATION`：形成方向或预测但交易许可未通过；只做影子观察。
  - `WATCHLIST`：有效交易结构和许可已形成，等待未来已收盘15分钟K线触发。
  - `EXECUTABLE`：触发、许可和账户风控全部通过；只有此类可进入模拟开仓。
- 新增 `v1_3-trade-gate-diagnostics.js`，把建议生成、影子验证、自动引擎和诊断保存收敛到 `processTradeGate()` 生产调用链。
- 新增 `ethAlphaTradeGateDiagnostics` 独立存储，保存最近1000条；同一已收盘K线按键更新投影，每次评估仍累积统计。
- 数据失败会写入当前“数据不可用”诊断，清空本轮许可、方案和评分字段，不沿用上一条有效结论。
- 增加许可枚举显式校验、秒/毫秒时间戳兼容、入场区上下界规范化、旧建议档案schema迁移。
- 页面新增完整中文诊断中心、拒绝统计及JSON/CSV导出；CSV复用V1.1公式注入防护。

## 接口核查结论

- `signalPermission.level`与V1.3读取枚举一致；新增显式未知枚举拒绝，避免未来静默漂移。
- `alignment`正式值为`full_aligned/partial_aligned/counter_trend/conflict`，风险档位读取正确，未改动风险比例。
- `triggerPlans.long/short`与生产读取一致；入场区字符串、数组及倒序数组均安全规范化。
- V1.2预测此前已经传入档案快照和自动引擎，本轮统一流水线继续保留。
- `sourceConfirmedBarTime`按已收盘15分钟K线进入指纹；跨K线生成新记录，同K线实时刷新去重。
- `validUntil`正式生产值为毫秒；本轮对旧数据或异常输入的秒值进行兼容转换。
- `v11decision`由每次成功REST刷新真实派发；内部引擎并非“显示运行但未调用”，而是此前在无档案候选时静默返回。

## 安全边界

- 不降低阈值，不强制修改 `worthBetting`，不绕过硬性否决。
- `OBSERVATION`和`WATCHLIST`不建立模拟仓位、不占用保证金。
- 不连接账户、不读取密钥、不发送真实订单。
- 未修改V1.1、V1.2核心算法文件。

## 环境说明

开始时本地 `main` 与现有 `origin/main` 均为 `353e28d`（V1.3 merge）。在线 `git fetch` 因本机HTTPS Git helper/Xcode环境不可用而失败；开发基于已同步的本地远端跟踪基线完成。
