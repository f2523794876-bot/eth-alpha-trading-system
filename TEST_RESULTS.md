# V1 测试结果

执行日期：2026-07-13（Asia/Shanghai）

## 汇总

| 类别 | 通过 | 失败 | 阻塞 |
|---|---:|---:|---:|
| V1既有合成行情与纯函数 | 38 | 0 | 0 |
| V1.1新增纯函数 | 17 | 0 | 0 |
| Claude复审定向计算/安全测试 | 15 | 0 | 0 |
| V1.1单文件界面、失效与恢复路径 | 12 | 0 | 0 |
| Binance 六路真实 REST与V1.1输出 | 8 | 0 | 0 |
| 真实浏览器 `file://` 双击形态 | 1 | 0 | 0 |
| 合计 | 91 | 0 | 0 |

V2/V3 项目 T16、T21、T22、T23 以及 T24.2-T24.5 按规范标记为不适用，未计入通过率。

## 关键结果

- P0 旧算法复现成功：包含当前突破K线的 high20 判定返回 false。
- 修复后冻结结构测试通过：确认K线排除在 priorStructureHigh20/Low20 之外，正式突破可进入 `BULL_CONFIRMATION`。
- 未收盘插针仅产生“盘中预警（未收盘，仅供参考）”，不改变正式状态；收盘回区间后仍不算突破。
- 三周期同向、15分钟逆4小时、BTC任一周期冲突、数据失效阻断均通过。
- 净盈亏比确认包含手续费、半价差与滑点，且小于毛盈亏比。
- 真实 Binance API：ETH/BTC × 15m/1h/4h 各返回120根K线，三周期决策成功生成，健康状态 `normal`。
- HTML 三段内嵌脚本均可被 JavaScript 编译器解析。

## 浏览器测试条件

尝试使用 Codex 内置真实浏览器直接打开：

`file:///Users/penn/Documents/GitHub/eth-alpha-trading-system/eth-dynamic-trading-dashboard.html`

Codex执行环境曾因安全策略拒绝本地 `file://` 导航。随后CEO已根据真实Chrome双击运行截图完成V1验收，确认六路REST、浏览器CORS与页面渲染通过。本轮不重复浏览器验收。

## 2026-07-13 验收修复回归

- 新增10项纯函数回归测试：支撑/压力Swing来源语义、历史压力转支撑标签、无交易方案RR文案、4小时过渡评分、1小时震荡评分、反向周期评分、靠近压力/支撑/区间中部拦截判断。
- 纯函数测试由28项增至38项，全部通过。
- 单文件中文映射与内嵌脚本编译检查通过。
- 六路真实REST再次通过7/7，健康状态 `normal`。

## V1.1 回归

- 双向待触发预案不会被当成当前建议，字段与固定免责声明完整。
- 多空评分独立，硬性数据否决可把可交易性强制归零。
- 九项条件进度、美元/ATR距离、区间百分比与进入区域判断通过。
- 15分钟倒计时起点/末秒边界及未收盘K线安全规则通过。
- Canvas图表数据严格取最近50根，支撑压力与分析区域一致；无有效方案时不含交易线。
- 最大亏损、建议试仓名义金额、成本、资金换算和加仓后超预算判断通过。
- 风险设置保存恢复、JSON/CSV导出、清除日志确认通过。
- 单文件新增区域、中文枚举、无WebSocket与无外部图表依赖检查通过。
- 六路真实REST回归8/8，包含真实数据下的双向预案、独立评分和50根图表模型。

## Claude复审定向回归

- 固定确认价并切换多个实时tick：正式状态、支撑压力、止损、目标、RR和双向预案逐字段保持一致；只有实时距离和盘中预警变化。
- 模拟成功面板后统一DOM失效：全部交易字段清空/标记失效，三项评分显示不可执行，最后已知价格明确标注非实时；生成的单文件包含恢复后移除遮罩并重建的路径。
- 高原始分叠加假突破失败、数据失效或多周期冲突时，均返回 `blocked=true`、具体原因、方向不可执行和可交易性0。
- 历史压力转支撑测试走真实K线→Swing→收盘突破→回踩→守住链路；无突破、盘中刺穿、跌回和回踩距离过远反例均不成立。
- CSV公式注入、独立可交易性公式与未连接持仓披露测试通过。

## 可复现命令

使用 Codex bundled Node：

```sh
node tests/v1-tests.js
node tests/v11-tests.js
node tests/audit-fixes-tests.js
node tests/v11-ui-tests.js
node tests/live-rest-test.js
```
