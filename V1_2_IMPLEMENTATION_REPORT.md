# ETH Alpha V1.2 走势预测与情景推演层实现报告

执行日期：2026-07-13（Asia/Shanghai）

## 基线与范围

- 稳定基线：`main` / `v1.1.0` / `d9289ef`。
- 规范基线：`origin/claude/v1.2-forecast-spec` / `3250192`。
- 实现分支：`codex/v1.2-forecast-layer`。
- `v1-core.js` SHA-256 保持 `0a4d9e712859d79ecae592aacffe371abfba29a2c6b7b76119a68c49e0471a97`，相对 `v1.1.0` 无差异。
- 未修改V1.1标签、Release或main历史；未实现V2回测、V3 WebSocket/提醒、账户连接、密钥读取或自动下单。

## 核心实现

新增 `v1_2-forecast-core.js`，以UMD形式单向只读依赖 `v1-core.js` 已导出的纯函数。预测层不发起网络请求，不访问DOM，不复制EMA、ATR、Swing、支撑压力或状态机实现。

三个预测时窗分别使用15分钟、1小时、4小时自身周期的已收盘 `confirmedPrice`、ATR和结构，同时读取ETH/BTC三个周期派生快照。未收盘价格不参与因子、权重、区间、目标或路径计算。

每个时窗固定计算12项因子：4小时趋势、1小时结构、15分钟结构、目标周期EMA斜率、Swing结构、ATR状态、支撑压力距离、目标周期成交量、BTC对应周期联动、对应周期假突破、区间位置和多周期冲突。

三档规则权重严格使用规范表格，列和均为100。缺失权重达到40、ATR无效、数据陈旧、关键ETH/BTC周期缺失或数据健康异常时直接降级为“数据不足”。规则权重归一化后总和恒为100，并通过版本校验和约束权重变化必须同步版本管理。

## 区间、情景与证据

- 规则型预计波动区间以对应周期 `confirmedPrice ± ATR×系数` 为基础，再使用方向正确且双边有效的支撑压力区域收紧或放宽；非法、倒挂或过窄结果回退至ATR区间。
- 偏多目标区始终位于确认价上方，偏空目标区始终位于确认价下方；区域无效时使用规范定义的ATR外推。
- 最可能路径按固定优先级选择突破回踩、跌破反抽、先回踩后上涨、先冲高后回落、区间往返或趋势延续。
- 支持证据与反对证据均来自12项因子的实际贡献点数，最多各3条。
- 置信度明确为数据完整度、方向领先优势和三周期一致性的内部评分，不代表胜率。

## 安全降级

- 手动观察模式：三个预测时窗全部为空，不生成权重、区间、目标、路径、失效条件或预测日志。
- 整体请求失败、关键周期缺失或V1.1健康检查早退：包装既有 `invalidateDashboard`，同步清空三个预测卡并将上一预测引用置空。
- 预测超过对应周期下一根K线收盘时间：整张预测卡替换为“预测已过期，等待刷新”，旧方向、权重、区间和目标数字不再显示。
- V1.1硬性否决与“不值得下注”结论原样并排展示；V1.2权重不能改变V1.1交易许可。

## 预测日志

正常、非手动、数据健康且未过期的每次生产页面预测会为15m、1h、4h分别建立并保存日志。日志key为独立的 `ethAlphaForecastLogs`，不会读写或清空V1.1决策日志。

唯一键由 `symbol + horizon + dataAsOf + forecastAlgorithmVersion + factorWeightVersion` 组成。同一根已收盘K线30秒刷新不会重复写入；新收盘K线、不同horizon、算法版本或权重版本允许新增。

容量常量 `FORECAST_LOG_LIMIT=1500`，超过后删除最旧记录。损坏JSON会安全恢复为空数组；localStorage不可用或容量超限会返回非阻塞错误并在页面显示警告，不影响预测渲染。

日志保存完整12项因子、方向权重、区间、目标、路径、失效条件、版本号和六路已收盘K线引用。V2结果、Brier、方向准确率、区间覆盖率与校准字段均保持 `null`。

## 验收结论

- V1.2新增自动化测试：278项通过，0失败。
- V1.1非联网回归：93项通过，0失败。
- 真实Binance REST：8项通过，0失败；真实行情下三个时窗与3条预测日志均成功生成。
- V1.1回归合计：93项非联网 + 8项真实REST = 101项通过，0失败。
- 人工Chrome验收：0项完成，1项环境阻塞。当前Codex环境无法连接Chrome扩展控制面，未伪造验收结果。

当前已达到代码、构建、自动化和真实REST交付标准；真实Chrome双击人工验收仍需在具备Chrome控制连接的环境执行。
