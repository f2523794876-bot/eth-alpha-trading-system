'use strict';const fs=require('node:fs'),assert=require('node:assert/strict');let p=0,f=0;function t(n,x){try{x();p++;console.log('PASS '+n)}catch(e){f++;console.error('FAIL '+n+': '+e.message)}}const h=fs.readFileSync(require('node:path').join(__dirname,'../eth-dynamic-trading-dashboard.html'),'utf8');for(const id of ['autoEngineSection','autoEngineState','autoAllowEntries','autoHeartbeat','autoGap','autoNextAction','autoArm','autoPause','autoResume','autoToggleEntries','autoDisarm'])t('自动引擎DOM '+id,()=>assert.match(h,new RegExp(`id="${id}"`)));for(const x of ['真实行情自动模拟交易','页面关闭或电脑休眠时不会实时运行','开启自动模拟交易（二次确认）','允许新开仓','最近心跳','数据缺口与恢复','下一自动动作'])t('中文引擎文案 '+x,()=>assert.match(h,new RegExp(x)));t('总开关真实二次确认',()=>assert.match(h,/armAutoEngine\(localStorage,\(\)=>confirm/));t('关闭引擎真实二次确认',()=>assert.match(h,/disarmAutoEngine\(localStorage,\(\)=>confirm/));t('重置账户真实二次确认',()=>assert.match(h,/resetPaperAccount\(localStorage,\(\)=>confirm/));t('紧急平仓和缺口结算不同入口',()=>{assert.match(h,/emergencyClosePosition/);assert.match(h,/confirmDataGapConservativeSettlement/)});console.log(`RESULT passed=${p} failed=${f}`);if(f)process.exitCode=1;
