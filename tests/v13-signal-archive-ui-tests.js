'use strict';const fs=require('node:fs'),assert=require('node:assert/strict');let p=0,f=0;function t(n,x){try{x();p++;console.log('PASS '+n)}catch(e){f++;console.error('FAIL '+n+': '+e.message)}}const h=fs.readFileSync(require('node:path').join(__dirname,'../eth-dynamic-trading-dashboard.html'),'utf8');for(const id of ['signalArchiveSection','signalArchiveList','signalExportJson','signalExportCsv','signalReset','shadowStats'])t('建议档案DOM '+id,()=>assert.match(h,new RegExp(`id="${id}"`)));for(const x of ['建议档案与影子验证','建议表现独立评估','不建立模拟账户仓位','不占用保证金','规则权重不是统计概率、胜率或保证','清空建议历史（二次确认）'])t('中文档案文案 '+x,()=>assert.match(h,new RegExp(x)));t('生产事件先建档再影子评估',()=>assert.match(h,/recordSignalIfEligible[\s\S]*evaluateShadowSignals[\s\S]*tickAutoEngine/));t('档案重置独立',()=>assert.match(h,/resetSignalArchive\(localStorage/));console.log(`RESULT passed=${p} failed=${f}`);if(f)process.exitCode=1;
